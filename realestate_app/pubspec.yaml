name: realestate_app
description: A new Flutter project for real estate platform.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  cached_network_image: ^3.3.0
  intl: ^0.19.0
  smooth_page_indicator: ^1.1.0
  url_launcher: ^6.2.2
  supabase_flutter: ^2.0.0
  image_picker: ^1.0.7
  dotted_border: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true